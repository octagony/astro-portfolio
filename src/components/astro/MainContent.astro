---
export interface Props {
	classname?: string
}

const { classname = '' } = Astro.props
---

<article class={`px-8 mb-6 mx-auto max-w-3xl font-hind font-medium leading-7 ${classname}`} transition:animate="slide">
	<slot />
</article>
